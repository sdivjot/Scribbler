import React from "react";

import NoteIcon from '@mui/icons-material/Note';

export default function Header() {
    return (
        <header>
            <h1>
                <NoteIcon/>
                Scribbler
            </h1>
        </header>

    );

}